// // use super::RoutingAlgo;

// pub struct LeastConnectionRouting {}


// impl LeastConnections {

//     pub fn new(sender_count: u32, thread_count: u32) -> Self {

//     }

// }

// impl RoutingAlgo for LeastConnections {
//     fn new(sender_count: u32, thread_count: u32) -> Self {}

//     fn process_job(&mut self, executor_function: crate::core::Job) {}

//     fn generate_threads<Job: FnOnce() + Send + 'static>(
//         // there can be 1 sender
//         // and n receivers depending on the number of threads
//         sender_count: u32,

//         // every thread will have 1 receiver
//         thread_count: u32,
//     ) -> super::ThreadPool<crate::core::Job> {
//     }
// }
